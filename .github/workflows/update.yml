name: Build sources and publish

on:
  push:
    branches: [trunk]
    paths:
      - src/**
      - blog-builder/*.js
      - blog-builder/*.json
      - highlight/index.js
      - highlight/*.json
      - highlight/build.rs
      - highlight/Cargo.toml
      - highlight/src/**
      - .github/workflows/update.yml
      - package.json

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: true
          ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "21"
      - name: Cache node modules
        uses: actions/cache@v2
        with:
          path: "node_modules"
          key: ${{ runner.os }}-modules-${{ hashFiles('package-lock.json') }}s
      - name: Cache blog node modules
        uses: actions/cache@v2
        with:
          path: "blog-builder/node_modules"
          key: ${{ runner.os }}-modules-${{ hashFiles('blog-builder/package-lock.json') }}s
      - name: Cache highlight node modules
        uses: actions/cache@v2
        with:
          path: "highlight/node_modules"
          key: ${{ runner.os }}-modules-${{ hashFiles('highlight/package-lock.json') }}s
      - name: Install dependencies
        run: |
          npm clean-install
          npm run build:pre
      - name: Build sources head
        run: npm run build

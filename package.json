{
  "name": "caellian-portfolio",
  "version": "2.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev --host",
    "preview": "vite preview --host",
    "install:blog": "npm install --prefix ./blog-builder",
    "preinstall": "npm run install:blog",
    "build:blog": "npm start --prefix ./blog-builder",
    "build:post-schema": "json2ts ./blog-builder/post.schema.json ./types/post.d.ts",
    "build:pre": "npm run build:blog && npm run build:post-schema",
    "build": "vite build",
    "check": "svelte-check",
    "check:watch": "svelte-check --watch",
    "lint": "prettier --check --plugin-search-dir=. . && eslint .",
    "format": "prettier --write --plugin-search-dir=. ."
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.3"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4",
    "@js-joda/core": "^5.6",
    "@sveltejs/adapter-static": "~3.0",
    "@sveltejs/kit": "~2.8",
    "cheerio": "^1.0.0",
    "eslint": "^9.15",
    "eslint-config-prettier": "^9.1",
    "eslint-plugin-svelte": "^2.46",
    "html-minifier": "^4.0",
    "prettier": "^3.3",
    "prettier-plugin-svelte": "^3.3",
    "stylus": "^0.64",
    "svelte": "~5.2",
    "svelte-check": "~4.1",
    "svelte-preprocess": "~6.0",
    "to-px": "^1.1",
    "vite": "^5.4"
  },
  "engines": {
    "node": ">=22.0",
    "npm": ">=10.0"
  }
}
